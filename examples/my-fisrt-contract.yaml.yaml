version: dev
description: 'Carica foto'

request:
    method: PATCH
    path: /api/v1/categorie/
    headers:
        Content-Disposition:
            form-data:
            name: immagine
            filename: image.jpg

response:
    status_code: 204
